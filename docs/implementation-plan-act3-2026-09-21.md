# Act 3 Asset & Flow Pass — Plan (2026-09-21)

**Roles:** Claude = PM (decisions, specs, review, verification). Antigravity (agy) agents = execution.
**Scope:** `js/act3-screen.js`, `css/act3-screen.css`, new files in `Assets/Image/`, Act 3 handoff in `js/app.js`.
**Source of truth:** Craft "Act 3" docs (`fefb2d16…`), `docs/design-language.md`, `DESIGN-REVIEW-act3.md`, Visual Asset Brief (`3ed42241…`).

## Current state (verified 2026-09-21)
- Act 3 = 8 beats: wait → verdict → report → tayReturn → nextTime → recheck → home → end.
- Lobby, Callie-waiting, Reyes and Tay-return art are **inline placeholder SVG** (`renderLobbyArt` et al., `js/act3-screen.js:747–1000`). Only `CallieAndTay-Vet.jpg` and `CallieAndTay-Home.jpg` are real plates.
- 31 `business:` stage directions are **rendered as readable bylines** (`.act3-business`, lines 943/957/1219). Must go.
- `DESIGN-REVIEW-act3.md` has 5 open findings (no act-level progress, clock ticks per line, case-file overflow, off-scale spacing, "Skip ahead" label).
- One implemented sticky note on `act3-wait-panel` (Reyes appears on delay) — confirm it actually shipped.

## Phases

### Phase 1 — Flow audit & trim (agy audit → Claude decides → agy edits)
1. agy produces a beat-by-beat interaction inventory: every click/keypress, what it reveals, whether it demands a decision or just "next".
2. Claude cuts/merges: candidates already visible — per-line clock tick (remove), verdict/report sub-steps that are pure "next" chains (collapse where no new information lands), 3E zero-state label, any leftover wait-panel interaction. Keep: the 3E commitment choice, case-file reveals, recheck decision, Tay-return payoff.
3. agy applies the trims + the five DESIGN-REVIEW fixes (progress pill, clock at beat boundaries only, case-file collapse, 4px spacing scale, "Nothing changes ▶").
**Gate:** Claude walks the act in the browser; step count reduced, no beat drops information the learner needs.

### Phase 2 — Asset spec & generation (Claude specs → user-approved tool → agy wires)
Shot list (all flat-vector, no outlines, warm palette, matching `CallieAndTay-Vet.jpg`):
1. **Lobby plate** — reception, one chair, door to back. No characters baked in.
2. **Callie waiting** (cutout, transparent) — 2 expressions: *drained/anxious* (wait) and *relief breaking* (verdict).
3. **Dr. Reyes** (cutout) — 3 poses/expressions: *chart-reading neutral*, *warm/reassuring*, *flat/serious* (survival numbers beat).
4. **Tay returning** (cutout) — cone on, alert, tongue in; plus one *trying to eat the cone* variant.
5. **Discharge sheet / chart prop** — for report + recheck beats.
Expression is selected in code per line from a new `mood`/`pose` key (replacing `business`), so direction never renders as text.
Existing `CallieAndTay-Home.jpg` and `-Vet.jpg` stay.
**Decision needed from you:** generation tool (Magnific / Adobe Firefly / other) — see questions below.
**Gate:** Claude reviews every image against style + expression-to-line match before wiring.

### Phase 3 — Integration & UI consistency (agy)
1. Replace placeholder SVG helpers with `<img>`/layered elements, keeping `data-editor-id`s so Edit Mode still works.
2. Strip `.act3-business` rendering; map `business` → `mood` per line.
3. HUD, dialogue bubble, buttons, stamps aligned to Act 1/2 patterns per `docs/design-language.md`.
**Gate:** Claude browser-walks every beat, both directions (prev/next), reduced-motion, keyboard-only.

### Phase 4 — Evaluation passes (agy drafts → Claude adjudicates → agy fixes)
1. UX heuristics (10 heuristics) on the finished act.
2. Persuasive UX (Fogg 7 tools) — focus on 3E commitment and the survival-number beat.
3. Debug pass: console errors, prev/next state integrity, handoff from Act 2, replay, Edit Mode overrides in `visual-overrides.css`.
Findings ranked; fix anything Medium+; log Low as follow-ups.
**Gate:** zero console errors, all findings dispositioned.

### Phase 5 — Ship
Commit per phase (`feat(act3): …`). Loop Phases 1–4 if the walkthrough surfaces regressions.

## Estimates (execution time, agy + my review)
- Phase 1: ~1 hr · Phase 2: ~1–2 hr (depends on generation tool) · Phase 3: ~1.5 hr · Phase 4: ~1 hr.

## Decisions (user, 2026-09-21)
1. Art is generated by **Antigravity** (`agy` `generate_image`, verified available on agy 1.2.7; fall back to the Antigravity desktop app if the CLI cannot match style). No Magnific spend.
2. `business` bylines: **remove the text**. Expressions still match beat tone via a small per-beat variant set (assumption — fewer variants than per-line).
3. Case file: **collapse** earlier panels into a chip row.
